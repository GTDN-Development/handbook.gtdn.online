---
title: "Container"
description: "Provides unified content width constraints across the app with fluid, responsive behavior."
highlight: true
---

import { Step, Steps } from "fumadocs-ui/components/steps";
import { TypeTable } from "fumadocs-ui/components/type-table";
import { Container } from "@/registry/v2/ui/container";


## Usage

```tsx
import { Container } from "@/components/ui/container";
```

```tsx
<Container>
  Wrapped content
</Container>
```

## Installation

<Steps>
    <Step>
        **Install dependencies**

        ```bash
        npm install radix-ui
        ```
    </Step>
    <Step>
        **Create the component file**

        ```bash
        touch src/components/ui/container.tsx
        ```
    </Step>
    <Step>
        **Copy & paste the source code**

        <include cwd lang="tsx" meta='title="components/ui/container.tsx"'>
          src/registry/v2/ui/container.tsx
        </include>

        Update the import paths to match your project setup.
    </Step>
</Steps>

## API Reference

<TypeTable
    type={{
    size: {
        type: '"sm" | "default" | "lg" | "prose" | "fluid"',
        description: ' width variants of the container',
    },
    asChild: {
        type: 'boolean',
        description: 'You can render container as a child element',
    },
    className: {
        type: 'string',
        description: 'Additional CSS classes',
    },
    }}
/>
